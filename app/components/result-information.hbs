{{#if (eq @winner "Draw")}}
  <p class="text-8xl font-black text-center">It's a draw!</p>
{{else}}
  <div class="text-8xl font-black">{{@winner}}</div>
  <div class="text-lg font-light">won!</div>
  {{confetti continuous=true}}
{{/if}}