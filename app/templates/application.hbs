{{page-title "TicTacToe"}}

<GameHeader />

{{! main layout, router wrapper }}
<div
  class="h-[calc(100vh_-_160px)] w-full flex flex-col justify-center items-center"
>
  {{outlet}}
</div>
{{! main layout, router wrapper }}